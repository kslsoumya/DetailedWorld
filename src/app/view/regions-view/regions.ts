export interface RegionOptions {
    region: string;
     resolution: string;
      colorAxis: object;
      backgroundColor: string;
      datalessRegionColor: string;
      defaultColor: string;
      displayMode: string;
      showTooltip: boolean;
    }
